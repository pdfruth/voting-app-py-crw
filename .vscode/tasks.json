{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "che",
            "command": "python3 -m venv .venv && . .venv/bin/activate && pip3 install --user -r requirements.txt",
            "workdir": "${PROJECTS_ROOT}/voting-app-py-ds",
            "component": "python",
            "problemMatcher": [],
            "label": "che: pip"
        },
        {
            "type": "che",
            "command": "python3 app.py",
            "workdir": "${PROJECTS_ROOT}/voting-app-py-ds",
            "component": "python",
            "problemMatcher": [],
            "label": "che: run"
        }
    ]
}